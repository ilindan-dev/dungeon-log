// Package ports defines the input and output contracts for the core business logic.
//
// This package should only contain Go interfaces and strictly rely on standard
// library packages or internal domain models. It must never import any
// external libraries or adapter implementations.
package ports
